-- Criando ScreenGui
local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local ESPButton = Instance.new("TextButton")
local TPButton = Instance.new("TextButton")
local AutoFarmButton = Instance.new("TextButton")
local NPCDropdown = Instance.new("TextButton")
local TextBox = Instance.new("TextBox")

ScreenGui.Parent = game.CoreGui

-- Frame principal
Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Frame.Size = UDim2.new(0, 250, 0, 300)
Frame.Position = UDim2.new(0.5, -125, 0.5, -150)

-- Botão ESP
ESPButton.Parent = Frame
ESPButton.Text = "ESP Baús/Frutas"
ESPButton.Size = UDim2.new(0, 200, 0, 40)
ESPButton.Position = UDim2.new(0, 25, 0, 20)
ESPButton.MouseButton1Click:Connect(function()
    print("ESP ativado")
    -- Código ESP aqui
end)

-- Botão TP
TPButton.Parent = Frame
TPButton.Text = "Teleportar Ilhas"
TPButton.Size = UDim2.new(0, 200, 0, 40)
TPButton.Position = UDim2.new(0, 25, 0, 70)
TPButton.MouseButton1Click:Connect(function()
    print("TP realizado")
    -- Código TP aqui
end)

-- Botão Auto Farm
AutoFarmButton.Parent = Frame
AutoFarmButton.Text = "Auto Farm"
AutoFarmButton.Size = UDim2.new(0, 200, 0, 40)
AutoFarmButton.Position = UDim2.new(0, 25, 0, 120)
AutoFarmButton.MouseButton1Click:Connect(function()
    print("Auto Farm iniciado")
    -- Código Auto Farm aqui
end)

-- Dropdown NPC/Boss (simples)
NPCDropdown.Parent = Frame
NPCDropdown.Text = "Selecionar NPC/Boss"
NPCDropdown.Size = UDim2.new(0, 200, 0, 40)
NPCDropdown.Position = UDim2.new(0, 25, 0, 170)
NPCDropdown.MouseButton1Click:Connect(function()
    print("NPC/Boss selecionado")
    -- Aqui você pode abrir outra UI com opções
end)

-- TextBox
TextBox.Parent = Frame
TextBox.PlaceholderText = "Digite algo..."
TextBox.Size = UDim2.new(0, 200, 0, 40)
TextBox.Position = UDim2.new(0, 25, 0, 220)
TextBox.FocusLost:Connect(function()
    print("Texto digitado: "..TextBox.Text)
end)
